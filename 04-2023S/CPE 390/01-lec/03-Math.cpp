int main() {
  // int not guaranteed size and no guaranteed sign
  {
    int a = 3 / 2;
    int b = 7 / 2;
    int c = 7 % 2;
    int d = 2 + 3;
    int e = 2 - 3;
  }
  {
    // what is a bit? 0/1 low voltage/high voltage
    // Arduino Uno 1=5v
    // Arduino Due 1=3.3v
    // What's high voltage on a Laptop? Find out for Homework
    uint8_t a = 0;
    uint16_t b = 65535;
    uint32_t c = 10000000;
    uint64_t d = 12345678ULL;

    int age;
    cin >> age;
    // Read in age and print it out in seconds.
    age * 12;
  }

  for (int i = 0; i <= 10; i++) {
  }
}

// Talk to Dov about internship and also doing extra work in the lab